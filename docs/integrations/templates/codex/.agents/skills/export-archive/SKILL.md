---
name: export-archive
description: Publish the local archive shell and reports shell for the current workspace.
disable-model-invocation: true
---

Run:

```bash
agent-exporter publish archive-index --workspace-root .
```

Return:

1. archive shell path
2. reports shell path
3. transcript count
4. report count
